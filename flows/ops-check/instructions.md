# instructions - ops check

- never say something is fine. only say what is wrong.
- keep the raw line, do not paraphrase it
- one finding per line, in this shape exactly:

      - [<sev>] <count>x <the raw line, cut at 120 chars>

- sev is one of high, med, low
- high means down or losing data, med means it will be high by friday, low
  means worth knowing but it can wait
- sort high first
- if unsure whether something counts, leave it out
