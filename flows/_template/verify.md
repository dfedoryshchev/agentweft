# verify

here is what this flow produced. do not improve it. check it against what the
flow promised, one line per promise:

    PASS <the promise>
    FAIL <the promise> - <what broke it>

if every promise passes, say so and stop. do not restate the output.
